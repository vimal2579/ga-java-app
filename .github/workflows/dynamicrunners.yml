name: Dynamic Runner

on: workflow_dispatch

vars:
  APP_TYPE: java   # change to 'dotnet'

jobs:
  build:
    runs-on: ${{ vars.APP_TYPE == 'java' && 'ubuntu-latest' || 'windows-latest' }}

    steps:
      - name: Show runner
        run: echo "Running on $RUNNER_OS"